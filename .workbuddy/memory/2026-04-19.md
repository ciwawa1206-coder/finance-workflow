# 2026-04-19 工作日志

## 完成事项

### GitHub推送
- 运行自动化测试：全部通过
- 提交更改：修复日历跨年月边界问题；添加农历黄历显示；优化日历布局
- 推送到GitHub：成功
- 提交ID: 38de9dd

### 修复日历不显示日期问题
- 问题：`solarToLunar` 函数中 `diffDays` 用 `const` 声明但后续被修改，导致JavaScript错误
- 解决：将 `const diffDays` 改为 `let diffDays`
- 提交ID: 877053f

### 修复农历计算不准确问题
- 问题：原算法使用1900-2050年近似数据表，计算结果不准确
- 解决：使用2020-2030年准确的春节日期和每月天数表
- 提交ID: bc8de2c

### 修复农历和节气显示问题
- 农历问题：2026年闰五月未正确处理，导致日期计算错误
- 节气问题：使用 `Math.abs(term.day - day) <= 1` 导致前后3天都显示同一节气
- 解决：
  - 农历数据结构增加闰月信息，正确处理闰月
  - 节气使用精确日期数据（2026年），只匹配当天
- 提交ID: 7d26a93

### 修复农历和节气重复问题
- 农历问题：时区问题导致日期计算偏差，显示"二月十五"而非"三月初三"
- 节气问题：`LunarData.solarTerms` 和 `solarTermDates` 同时匹配，导致清明显示两天
- 解决：使用 `Date.UTC()` 计算，删除近似数据匹配
- 提交ID: c88b5c4

### 修复农历差一天问题（三月初二→三月初三）
- 根本原因：旧代码残留了全局函数 `leapDays()` 和 `lunarMonthDays()`，与新代码中的局部变量同名，导致变量被全局函数覆盖，计算出错
- 解决：删除旧的全局辅助函数（lunarYearDays/leapDays/lunarMonthDays/leapMonthOfYear），重命名局部变量避免冲突
- 提交ID: c836fd9

### 修复日历格子空白 + 农历头部显示
- 农历头部问题：`updateLunarInfo` 始终传当月1日，应改为本月时传今天
- 日历空白问题：`solarToLunar` 返回null时 `lunar.day` 抛TypeError，循环中断，格子没有渲染
- 解决：加 `|| { day: 1, monthName: '', dayName: '' }` 空值防护；农历头部改为本月显示今天
- 提交ID: 357ab0f

## 网站更新内容
1. 日历同时显示阳历和农历
2. 显示黄历宜忌信息
3. 显示24节气
4. 修复1月和12月日期显示问题
5. 优化日历布局，增大显示区域
6. 添加月份切换功能
7. 添加日期选择和行程查看功能
8. 修复农历计算准确性

## 在线地址
https://ciwawa1206-coder.github.io/finance-workflow/
